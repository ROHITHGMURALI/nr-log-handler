[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "nr-log-handler"
version = "0.1.0"
description = "Send logs to New Relic via their Log REST API"
readme = "README.md"
license = "MIT"
authors = [{ name = "Rohith G Murali", email = "rohith.rgm.murali@gmail.com" }]
requires-python = ">=3.10"
dependencies = ["requests>=2.28"]
keywords = ["newrelic", "logging", "observability"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: System :: Logging",
]

[project.urls]
Homepage = "https://github.com/ROHITHGMURALI/nr-log-handler"
Repository = "https://github.com/ROHITHGMURALI/nr-log-handler"

[project.optional-dependencies]
dev = ["pytest>=7.4", "pytest-mock>=3.11"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
